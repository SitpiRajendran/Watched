<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Watched - Movie Library</title>
    <link href="https://fonts.googleapis.com/css2?family=Nunito+Sans:wght@400;500;700&display=swap" rel="stylesheet">
    <script src="https://kit.fontawesome.com/15181efa86.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="https://unpkg.com/bulma@0.9.3/css/bulma.min.css" />
    <link rel="stylesheet" href="/styles.css" />
  </head>

  <body>
    <section class="container">
      <div class="columns is-multiline">
        <div class="column is-8 is-offset-2 register">
          <div class="columns">
            <div class="column left">
              <h1 class="title is-1">Watched</h1>
              <h2 class="subtitle colored is-4">What did you watch today ?</h2>
              <p>Share with your friends your movies interest</p>
            </div>
            <div class="column right has-text-centered">
              <h1 class="title is-4">Login</h1>
              <div class="field mt-4">
                        <% if(typeof error != 'undefined' ) {%>
                            <div class="notification is-danger p-3 has-text-centered">
                                <%= error %>
                            </div>
                            <% } %>
                                <div class="container is-danger">
                                    <% %>
                                </div>
                                <form action="/login" method="POST">
                                    <p class="control has-icons-right mb-4">
                                        <input class="input is-medium has-background-white-ter is-white" type="text"
                                            id="email" name="email" placeholder="Email">
                                        <span class="icon is-right">
                                            <i class="fas fa-user"></i>
                                        </span>
                                    </p>
                                    <p class="control has-icons-right mb-4">
                                        <input class="input is-medium has-background-white-ter is-white" type="password"
                                            id="password" name="password" placeholder="Mot de Passe">
                                        <span class="icon is-right">
                                            <i class="fas fa-lock"></i>
                                        </span>
                                    </p>
                                    <button type="submit" class="button is-primary is-medium" style=" width: 100%;">C'est
                                        parti !</button>
                                </form>
                                <a href="/register">
                                    <p class="subtitle is-6 has-text-right mt-3">You need an account ?</p>
                                </a>
                    </div>
            </div>
          </div>
        </div>
      </div>
    </section>
  </body>
  <style>
    :root {
      --brandColor: hsl(258, 67%, 51%);
      --background: rgb(247, 247, 247);
      --textDark: hsla(0, 0%, 0%, 0.66);
      --textLight: hsla(0, 0%, 0%, 0.33);
    }
    body {
      background: var(--background);
      height: 100vh;
      color: var(--textDark);
    }
    .field:not(:last-child) {
      margin-bottom: 1rem;
    }
    .register {
      margin-top: 10rem;
      background: white;
      border-radius: 10px;
    }
    .left,
    .right {
      padding: 4.5rem;
    }
    .left {
      border-right: 5px solid var(--background);
    }
    .left .title {
      font-weight: 800;
      letter-spacing: -2px;
    }
    .left .colored {
      color: var(--brandColor);
      font-weight: 500;
      margin-top: 1rem !important;
      letter-spacing: -1px;
    }
    .left p {
      color: var(--textLight);
      font-size: 1.15rem;
    }
    .right .title {
      font-weight: 800;
      letter-spacing: -1px;
    }
    .right .description {
      margin-top: 1rem;
      margin-bottom: 1rem !important;
      color: var(--textLight);
      font-size: 1.15rem;
    }
    .right small {
      color: var(--textLight);
    }
    input {
      font-size: 1rem;
    }
    input:focus {
      border-color: var(--brandColor) !important;
      box-shadow: 0 0 0 1px var(--brandColor) !important;
    }
    .fab,
    .fas {
      color: var(--textLight);
      margin-right: 1rem;
    }
  </style>

</html>